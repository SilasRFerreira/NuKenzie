import styled from 'styled-components';

export const StyledEntrySection = styled.section`
    font-family: var(--nunito-family);

    width: clamp(200px, 100%, 355px);
    
    display: flex;
    flex-direction: column;

    gap: 2rem;
`